import React from "react";

const SplashScreen = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "24px",
        background: "#fff",
      }}
    >
      Loading...
    </div>
  );
};

export default SplashScreen;
